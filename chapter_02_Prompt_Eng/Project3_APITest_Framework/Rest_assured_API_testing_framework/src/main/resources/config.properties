# -----------------------------------------------------------------------
# Framework configuration — JSONPlaceholder public API
# Values here are the fallback. Override via env variables or JVM -D flags:
#   BASE_URL / base.url
#   AUTH_TYPE / auth.type   (bearer | apikey | basic | leave blank for none)
#   AUTH_TOKEN / auth.token (value injected by CI; never commit real tokens)
# -----------------------------------------------------------------------
base.url=https://jsonplaceholder.typicode.com
auth.type=
auth.token=
